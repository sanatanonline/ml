def depth_first(nodes):
    """Pass a list of nodes and the function
    navigates through it to generate the DFS path"""
    if nodes is not None:
        print("nodes: ", nodes)
    else:
        print("invalid nodes")
